console.log(10 % 3);//result is 1 because 10 mod 3 is equal 1
console.log(5 === "5");// the reason that 5==="5" is false because 5 with "" is the string.and for === it think about 3 reason,although it has the same value.
console.log(5 == "5"); // for == it only checks the same value ,it don't care about anythings.
console.log(2 ** 3);// 2**3 it mean that 2 power 3 is equal 8.
console.log(null ?? "default");//If the value on the left is null or undefined, use the value on the right.
console.log(0 || "fallback");//If the value on the left is falsy, use the value on the right.
console.log(!true && false); // !true is equal falsed,so false and false are false.
